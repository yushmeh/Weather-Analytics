# Weather Analytics — Погодная аналитика Москвы

Python-проект: получение данных о погоде через открытый API, сохранение в базу данных SQLite, расчёт аналитических метрик и интерактивная визуализация результатов.

---

## Содержание

- [Описание](#описание)
- [Стек технологий](#стек-технологий)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Что делает скрипт](#что-делает-скрипт)
- [Аналитические метрики](#аналитические-метрики)
- [Схема базы данных](#схема-базы-данных)
- [Дашборд](#дашборд)
- [Обработка ошибок](#обработка-ошибок)

---

## Описание

Скрипт каждый раз при запуске:

1. делает GET-запрос к [Open-Meteo API](https://open-meteo.com/) и получает температуру за последние 7 дней для Москвы
2. парсит JSON-ответ и сохраняет данные в локальную базу данных SQLite (`weather.db`)
3. рассчитывает аналитические метрики и выводит их в консоль
4. строит интерактивный двухпанельный график и сохраняет его как `weather_chart.html`

Повторный запуск безопасен: данные обновляются через `UPSERT`, дубликатов не возникает.

---

## Стек технологий

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.14 |
| HTTP-запросы | `requests` |
| База данных | SQLite 3 (встроенный модуль `sqlite3`) |
| Визуализация | `plotly` (offline, HTML-файл) |
| Дашборд | HTML + CSS + JavaScript + Chart.js |
| API | [Open-Meteo](https://open-meteo.com/) (бесплатно, без ключа) |

---

## Структура проекта

```
Day4/
├── main.py                # Основной скрипт
├── weather_dashboard.html # Интерактивный дашборд (тёмная/светлая тема)
├── weather_chart.html     # График, генерируется при запуске main.py
├── weather.db             # База данных SQLite, создаётся автоматически
└── README.md
```

---

## Установка и запуск

### 1. Клонировать / скачать проект

```bash
git clone https://github.com/your-username/weather-analytics.git
cd weather-analytics
```

### 2. Создать виртуальное окружение (рекомендуется)

```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# macOS / Linux
source .venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install requests plotly
```

> `sqlite3` входит в стандартную библиотеку Python — устанавливать отдельно не нужно.

### 4. Запустить скрипт

```bash
python main.py
```

### 5. Посмотреть результаты

- Аналитика выводится прямо в консоль
- График открывается в браузере: откройте файл `weather_chart.html`
- Полный дашборд: откройте файл `weather_dashboard.html`

---

## Что делает скрипт

```
main.py
│
├── fetch_weather_data()       GET-запрос к Open-Meteo API
├── parse_weather_records()    Парсинг JSON → список кортежей
│
├── get_connection()           Подключение к SQLite
├── create_table()             CREATE TABLE IF NOT EXISTS weather_log
├── save_records()             INSERT OR REPLACE (UPSERT)
├── load_records()             SELECT всех записей
│
├── calculate_analytics()      Расчёт метрик из списка записей
├── print_analytics()          Вывод в консоль (таблица + метрики)
│
└── build_chart()              Plotly-график → weather_chart.html
```

**API-запрос** (Москва, 7 прошедших дней):

```
GET https://api.open-meteo.com/v1/forecast
    ?latitude=55.75
    &longitude=37.62
    &daily=temperature_2m_max,temperature_2m_min
    &timezone=Europe/Moscow
    &past_days=7
```

---

## Аналитические метрики

После запуска в консоли отображается:

| Метрика | Описание |
|---------|----------|
| Средняя максимальная температура | Среднее `temp_max` за 7 дней |
| Средняя минимальная температура | Среднее `temp_min` за 7 дней |
| Самый тёплый день | Дата с наибольшим `temp_max` |
| Самый холодный день | Дата с наименьшим `temp_min` |
| Температурный размах | `max(temp_max) − min(temp_min)` за весь период |

Пример вывода:

```
─────────────────────────────────────────────
  📊  АНАЛИТИКА ПОГОДЫ ЗА 7 ДНЕЙ (Москва)
─────────────────────────────────────────────
  Средняя максимальная температура : +21.5 °C
  Средняя минимальная температура  : +13.3 °C
─────────────────────────────────────────────
  🌡️  Самый тёплый день  : 2026-05-21  (+31.2 °C)
  🥶  Самый холодный день: 2026-05-27  (+7.7 °C)
─────────────────────────────────────────────
  📐  Температурный размах за период: 23.5 °C
─────────────────────────────────────────────

  Дата          Макс.    Мин.
  ────────────────────────────
  2026-05-21   +31.2   +20.0
  2026-05-22   +29.5   +16.7
  ...
```

---

## Схема базы данных

База данных `weather.db` содержит одну таблицу:

```sql
CREATE TABLE IF NOT EXISTS weather_log (
    date     TEXT PRIMARY KEY,   -- формат YYYY-MM-DD
    temp_max REAL NOT NULL,      -- максимальная температура за день, °C
    temp_min REAL NOT NULL       -- минимальная температура за день, °C
);
```

Поле `date` является первичным ключом — повторный запуск не создаёт дубликаты, а обновляет существующие записи (`INSERT OR REPLACE`).

---

## Дашборд

Файл `weather_dashboard.html` — полноценный интерактивный дашборд, не требующий запуска Python:

- **Тёмная / светлая тема** — кнопка переключения в шапке
- **5 карточек метрик** — avg max/min, самый тёплый/холодный день, размах
- **График Chart.js** — линии макс/мин с заливкой + столбцы диапазона на отдельной оси
- **Интерактивная таблица** — сортировка по любому столбцу, визуальные полоски диапазона, бейджи для крайних значений
- **Адаптивность** — корректно отображается на мобильных устройствах
- **Анимации** — плавное появление карточек и строк таблицы при загрузке

Чтобы подключить дашборд к реальным данным из `weather.db`, добавьте в `main.py` генерацию HTML с вставкой данных через `json.dumps()`:

```python
import json

records_json = json.dumps([
    {"date": r[0], "temp_max": r[1], "temp_min": r[2]}
    for r in db_records
])
# Замените строку в weather_dashboard.html:
# const weatherData = <сюда вставить records_json>;
```

---

## Обработка ошибок

| Ситуация | Поведение |
|----------|-----------|
| Нет интернета | Сообщение об ошибке + завершение |
| Таймаут API (> 10 с) | Сообщение об ошибке + завершение |
| Неверный HTTP-статус | Вывод кода ошибки + завершение |
| Неожиданная структура JSON | Указание отсутствующего ключа + завершение |
| Ошибка SQLite | Подробное сообщение + завершение |
| Пустой ответ API | Сообщение об ошибке + завершение |

---

## Лицензия

Проект создан в учебных целях. Данные предоставляются сервисом [Open-Meteo](https://open-meteo.com/) по лицензии [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/).
